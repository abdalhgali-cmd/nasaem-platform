# تقرير الأخطاء ومقترح التطوير — منصة نسائم الحرمين (Nasaem Platform)

> تاريخ الإعداد: 2026-08-04 (آخر تحديث: نفس اليوم بعد جولة تنفيذ فعلية)
> نطاق المراجعة: المستودع الكامل (backend + frontend + إعدادات Docker) اعتمادًا على الفرع `main` حتى الالتزام `17e11a0`.

---

## 0. حالة التنفيذ

تم تنفيذ واختبار الإصلاحات التالية فعليًا (مع تشغيل خادم PostgreSQL حقيقي محليًا والتحقق بطلبات HTTP فعلية، وليس فقط قراءة الكود):

- ✅ البند 1: إنشاء أول Prisma Migration فعلية + `docker-compose.yml` يُشغّل `prisma migrate deploy`
- ✅ البند 2 و3: تصحيح `DATABASE_URL` الافتراضي + توثيق `.env` في `README.md`
- ✅ البند 5: توليد أرقام تسلسلية ذرية عبر نموذج `Counter` جديد (`utils/sequence.js`)
- ✅ البند 6: استبعاد الدفعات `REFUNDED` من حساب حالة الدفع — **واكتُشفت أثناء الاختبار مشكلة أعمق (انظر البند 20 أدناه) وتم إصلاحها أيضًا**
- ✅ البند 7: آلة حالات (State Machine) لانتقالات حالة الطلب، تُرجع `409` عند انتقال غير مسموح
- ✅ البند 8: حذف `role.middleware.js` المكرر
- ✅ البند 9: رفع ملفات حقيقي عبر `multer` — **واكتُشفت أثناء التنفيذ ثغرة إضافية (انتحال هوية عبر `uploadedById`) وتم إصلاحها أيضًا**
- ✅ البند 10: Rate Limiting مخصص لتسجيل الدخول
- ✅ البند 14 و15: مزامنة صلاحية كوكي المصادقة مع `JWT_EXPIRES_IN` + إضافة `POST /api/auth/logout`
- ✅ البند 16: كلمة مرور المسؤول الافتراضية من `SEED_ADMIN_PASSWORD` إجباريًا
- ✅ **إصلاح إضافي غير مذكور سابقًا** (بند 20 و21 أدناه): تسريب `passwordHash` عبر علاقات المستخدم، وخطأ عزل معاملات (Transaction) في حساب حالة الدفع

**جولة تنفيذ ثانية (نفس اليوم):**

- ✅ البند 11: Pagination (`page`/`limit`) على الطلبات، العملاء، الدفعات، المستندات، الخدمات — مع تقليل الـ `include` العميق في القوائم (الطلبات لم تعد تجلب `items`/`payments`/`history` كاملة في شاشة القائمة)
- ✅ البند 12 (جزئيًا): `PATCH` للعملاء، الفروع، الموردين، العروض، الخدمات؛ و`DELETE` للمستندات والخدمات (مع حماية: تعطيل بدل حذف فعلي إذا كانت الخدمة مستخدمة في طلبات سابقة). لم يُضف حذف للعملاء/الطلبات/الدفعات عمدًا (سجلات مالية/تشغيلية تاريخية يُفضّل عدم حذفها فعليًا)
- ✅ البند 13: تقييد `priority` (`LOW/NORMAL/HIGH/URGENT`) و`currency` (قائمة عملات مدعومة) عبر Zod enum في كل الـ validators ذات الصلة، دون تعديل مخطط قاعدة البيانات
- ✅ **فجوة إضافية اكتُشفت ولم تكن موثّقة سابقًا**: لم توجد وحدة (Module) لإدارة الخدمات (Services) إطلاقًا رغم أن كل طلب يعتمد على `serviceId` — أُضيفت وحدة `services` كاملة (`GET`/`POST`/`PATCH`/`DELETE`)

**جولة تنفيذ ثالثة (نفس اليوم) — بناء الواجهة الأمامية:**

- ✅ البند 4: بُنيت واجهة أمامية فعلية بـ HTML/CSS/JS عادي (بدون إطار عمل) تخدَّم من نفس خادم الـ Express (نفس المصدر Same-Origin، فلا حاجة لإعداد CORS معقّد؛ المصادقة تعتمد على كوكي `httpOnly` الموجود مسبقًا):
  - `login.html` — تسجيل دخول الموظفين
  - `request.html` — إنشاء طلب جديد: اختيار نوع الخدمة من `services-data.js` الموجود أصلًا ← عرض قائمة المستندات المطلوبة لكل نوع فرعي ← بيانات العميل ← التفاصيل المالية ← رفع المستندات فعليًا بعد إنشاء الطلب
  - `admin-dashboard.html` — نظرة عامة (إحصائيات)، الطلبات (قائمة مُرقّمة + فلترة بالحالة + شاشة تفاصيل مع تغيير الحالة وتسجيل الدفعات)، العملاء (قائمة + بحث)، الدفعات (قائمة) — كل تبويب يظهر/يختفي حسب صلاحية الدور (`role`) فعليًا
  - تم اختبار التدفق الكامل فعليًا عبر متصفح Chromium حقيقي (Playwright): تسجيل دخول → إنشاء طلب → رفع مستند → تغيير حالة → تسجيل دفعة → تسجيل خروج، مع لقطات شاشة تؤكد أن الواجهة تُعرض بشكل صحيح (RTL عربي سليم)
- ✅ **فجوة إضافية اكتُشفت أثناء بناء الواجهة (وأُصلحت)**: لم يكن ممكنًا فعليًا إنشاء طلب جديد لأن `serviceId` في `services-data.js` (تصنيفات الطلبات) لم يكن مرتبطًا بأي سجل `Service` حقيقي في قاعدة البيانات — تم توسيع `prisma/seed.js` ليزرع 9 سجلات `Service` (بأسعار أساس صفرية يُدخل الموظف السعر الفعلي عند الطلب) تطابق تصنيفات `services-data.js`، مع إصلاح السكربت ليعمل زرع الخدمات في كل مرة (كان الكود القديم يتوقف مبكرًا إذا كان حساب المسؤول موجودًا بالفعل، فيتخطى زرع الخدمات أيضًا)
- ✅ **خطأ إضافي اكتُشف أثناء اختبار الواجهة فعليًا (وأُصلح)**: عند إدخال قيمة مكررة لحقل فريد (مثل رقم جواز عميل موجود مسبقًا)، كان الخادم يُعيد خطأ Prisma الخام كنص كامل مع كود حالة `500` بدل رسالة واضحة — تم تعميم معالجة أخطاء Prisma الشائعة (تكرار قيمة فريدة، سجل غير موجود، تعارض علاقات) في `error.middleware.js` لإرجاع رسائل نظيفة وأكواد حالة صحيحة (`409`/`404`) بدل تسريب تفاصيل داخلية
- ✅ **خطأ تنسيق تواريخ اكتُشف بالفحص البصري للواجهة**: التواريخ كانت تُعرض بالتقويم الهجري تلقائيًا (سلوك افتراضي لـ locale `ar-SA` في المتصفح) رغم أنه نظام تشغيلي/تجاري يفترض عرض التقويم الميلادي — تم تثبيت `ca-gregory` صراحة في تنسيق التاريخ

**لم يُنفَّذ بعد** (مؤجل لمناقشة نطاقه أو لجولة لاحقة): شاشات إدارية إضافية في الواجهة (الفروع، الموردين، الخدمات، العروض، الإعدادات، المستخدمين — متاحة عبر الـ API لكن بدون واجهة رسومية بعد)، تفعيل `ActivityLog`/`Notification` (بند 17)، الاختبارات الآلية وCI (بند 18-19).

---

## 1. ملخص تنفيذي

تمت مراجعة كامل الكود المصدري للمنصة (باك-إند Node.js/Express + Prisma + PostgreSQL، وواجهة أمامية شبه معدومة). المشروع في مرحلة تأسيسية (Bootstrap) ويحتوي على بنية باك-إند جيدة التنظيم (Modules منفصلة لكل كيان)، لكن توجد أخطاء حرجة تمنع تشغيل النظام من الأساس، بالإضافة إلى ثغرات منطقية في حسابات الدفع، وفجوات كبيرة في الوظائف الأساسية (لا يوجد Frontend فعلي، ولا رفع ملفات حقيقي، ولا تعديل/حذف للبيانات).

تم تصنيف الأخطاء حسب الخطورة، يليها مقترح تطوير مرحلي.

---

## 2. الأخطاء المستخرجة

### 🔴 حرجة — تمنع تشغيل النظام

| # | الخطأ | الموقع | الأثر |
|---|-------|--------|-------|
| 1 | لا يوجد أي مجلد `prisma/migrations`، ولا يقوم `docker-compose.yml` بتشغيل `prisma migrate deploy` أو `prisma db push` — فقط `npx prisma generate` (الذي يُولّد Client فقط ولا ينشئ جداول). | `docker-compose.yml:26`، غياب `backend/prisma/migrations/` | قاعدة البيانات تبقى فارغة تمامًا بدون جداول عند أول تشغيل؛ أي طلب API يفشل بخطأ من Prisma (`relation does not exist`). هذا هو السبب الأرجح وراء سلسلة الالتزامات الأخيرة (`Fix Prisma startup`, `Fix Dockerfile`...) التي لم تحل المشكلة الجذرية بعد. |
| 2 | `docker-compose.yml` يشير إلى `env_file: ./backend/.env`، لكن هذا الملف غير موجود في المستودع (فقط `.env.example`). | `docker-compose.yml:20-21` | تشغيل `docker compose up` يفشل فورًا ما لم ينشئ المستخدم الملف يدويًا — خطوة غير موثّقة في `README.md`. |
| 3 | حتى بعد نسخ `.env.example`، القيمة الافتراضية لـ `DATABASE_URL` تستخدم `localhost` كمضيف لقاعدة البيانات. | `backend/.env.example:3` | داخل شبكة Docker Compose يجب استخدام اسم الخدمة `postgres` وليس `localhost`، وإلا فشل اتصال الباك-إند بقاعدة البيانات حتى بعد حل المشكلتين أعلاه. |
| 4 | لا يوجد Frontend فعلي في المستودع. ملف `frontend/assets/services-data.js` يشير صراحةً (في تعليق داخل الملف) إلى صفحتي `request.html` و`admin-dashboard.html` كمستخدمين لهذا الملف، لكن هاتين الصفحتين — وأي HTML/CSS/JS آخر — غير موجودتين إطلاقًا. | `frontend/` (يحتوي فقط على ملف بيانات واحد) | لا توجد واجهة استخدام فعلية للنظام حاليًا؛ الـ API وحده لا يكفي لتشغيل المنصة. |

### 🟠 عالية — أخطاء منطقية تؤثر على صحة البيانات

| # | الخطأ | الموقع | الأثر |
|---|-------|--------|-------|
| 5 | توليد الأرقام التسلسلية (رقم الطلب، رقم العميل، الرقم الوظيفي) يعتمد على `prisma.model.count() + 1` بدل معرّف متسلسل ذري (Atomic Sequence). | `orders.service.js` (`generateOrderNumber`)، `customers.service.js` (`generateCustomerNo`)، `users.service.js` (`generateEmployeeNo`) | في حال وصول طلبين متزامنين (Race Condition)، أو حذف سجل مستقبلًا، يمكن أن يتكرر نفس الرقم فيفشل قيد `@unique`، أو يحدث تضارب في الترقيم. |
| 6 | حساب حالة دفع الطلب (`recalculateOrderPaymentStatus`) يجمع مبالغ **كل** الدفعات المرتبطة بالطلب بغض النظر عن حقل `status` الخاص بكل دفعة. | `payments.service.js:8-34` | دفعة بحالة `REFUNDED` (مسترجعة) لا تُستبعد من المجموع، فيظل الطلب يظهر "مدفوع بالكامل" (`PAID`) رغم استرجاع المبلغ فعليًا — خطأ محاسبي مباشر. |
| 7 | لا يوجد أي تحقق من صحة الانتقال بين حالات الطلب (State Machine) عند تغيير الحالة. | `orders.service.js` (`updateOrderStatus`) | يمكن نقل أي طلب من `COMPLETED` مباشرة إلى `NEW` أو أي حالة أخرى دون قيود منطقية، ما يفتح الباب لتلاعب أو أخطاء تشغيلية. |
| 8 | دالة `requireRole` مكرّرة حرفيًا في ملفين مختلفين. | `middleware/auth.middleware.js` و `middleware/role.middleware.js` (نفس الكود تمامًا) | خطر واضح: تعديل الصلاحيات في ملف واحد فقط مستقبلًا سيترك الملف الآخر بمنطق قديم متعارض. |
| 9 | حزمة `multer` مُدرجة ضمن الاعتماديات لكنها غير مُستخدَمة في أي مكان بالكود؛ وحدة المستندات تستقبل `storagePath` كنص خام من العميل مباشرة دون أي رفع أو تحقق فعلي من الملف على الخادم. | `package.json`، `documents.controller.js` / `documents.service.js` | لا يوجد فعليًا رفع حقيقي لصور الجوازات/المستندات رغم أنها من صميم عمل شركة سفر وسياحة؛ أي عميل واجهة برمجية يمكنه إرسال أي مسار نصي عشوائي. |
| 10 | لا يوجد حد معدل طلبات (Rate Limiting) خاص بمسار تسجيل الدخول `/api/auth/login`. | `app.js` (حد عام 200 طلب/15 دقيقة لكل الـ API) | ضعف في الحماية من هجمات تخمين كلمة المرور (Brute Force) على حسابات الموظفين. |

### 🟡 متوسطة — فجوات وظيفية وجودة كود

| # | الخطأ | الموقع | الأثر |
|---|-------|--------|-------|
| 11 | غياب تام لأي ترقيم صفحات (Pagination) في كل القوائم (`listOrders`, `listCustomers`, `listPayments`, `listDocuments`, `listBranches`...)، مع `include` عميق للعلاقات في كل استعلام. | جميع ملفات `*.service.js` | مشاكل أداء حتمية مع نمو البيانات (جلب آلاف السجلات دفعة واحدة). |
| 12 | لا توجد عمليات تعديل (Update) أو حذف (Delete) لمعظم الكيانات (العملاء، الفروع، الموردين، المستندات، العروض) — فقط إنشاء وعرض. | جميع `*.routes.js` | أي خطأ إدخال لا يمكن تصحيحه إلا بالوصول المباشر لقاعدة البيانات. |
| 13 | حقول نصية حرة بدون قيود Enum: `priority` في `Order`، و`currency` في عدة نماذج، رغم وجود نموذج `Currency` منفصل بالفعل في قاعدة البيانات. | `prisma/schema.prisma`، ملفات `*.validators.js` | يسمح بإدخال قيم غير متسقة (مثلاً `currency: "usd$"` أو أولوية بأي نص). |
| 14 | مدة صلاحية كوكي المصادقة ثابتة يدويًا (`maxAge: 7 * 24 * 60 * 60 * 1000`) ولا ترتبط بمتغير البيئة `JWT_EXPIRES_IN` المستخدم فعليًا في توقيع التوكن. | `auth.controller.js` (`sendAuthCookie`) | إذا غُيّرت مدة صلاحية التوكن عبر البيئة، يبقى الكوكي بمدة مختلفة، ما يسبب سلوك مصادقة غير متوقع (كوكي موجود لتوكن منتهي، أو العكس). |
| 15 | لا توجد نقطة نهاية لتسجيل الخروج (Logout) لمسح كوكي المصادقة من جهة الخادم. | `auth.routes.js` | المستخدم لا يستطيع إنهاء جلسته بشكل نظيف عبر الـ API. |
| 16 | كلمة مرور المسؤول الافتراضية مكتوبة مباشرة في كود البذر (Seed) بدل قراءتها من متغير بيئة، بدون إجبار على تغييرها عند أول دخول. | `prisma/seed.js` (`Admin@12345`) | مخاطرة أمنية إذا نُسي تغيير كلمة المرور بعد النشر الفعلي. |
| 17 | نموذجا `ActivityLog` و`Notification` معرَّفان بالكامل في مخطط قاعدة البيانات لكن لا يوجد أي كود في كامل الباك-إند يكتب إليهما أو يقرأ منهما. | `prisma/schema.prisma`، بحث شامل في `src/` | ميزتا سجل النشاط والإشعارات غير مُفعَّلتين فعليًا رغم وجود البنية التحتية لهما. |

### 🔴 حرجة إضافية — اكتُشفت أثناء التنفيذ والاختبار الفعلي (تم إصلاحها)

هذان الخطآن لم يظهرا في المراجعة الأولى بقراءة الكود فقط؛ ظهرا عند تشغيل النظام فعليًا ضد قاعدة بيانات حقيقية واختبار السيناريوهات:

| # | الخطأ | الموقع | الأثر |
|---|-------|--------|-------|
| 20 | `recalculateOrderPaymentStatus` كانت تستخدم العميل العام `prisma` بدل عميل المعاملة `tx` الممرَّر داخل `prisma.$transaction(async (tx) => ...)`. | `payments.service.js` (`createPayment`) | نتيجة لذلك كانت الدالة تستعلم عن قاعدة البيانات **قبل اكتمال المعاملة (Transaction)**، فلا ترى الدفعة التي أُنشئت للتو داخل نفس المعاملة، فتحسب حالة الدفع بناءً على بيانات قديمة. تم تأكيد هذا فعليًا: بعد دفع كامل المبلغ، كان الطلب يبقى `UNPAID` بدل `PAID`. الإصلاح: تمرير `tx` إلى الدالة واستخدامه في كل الاستعلامات داخلها. |
| 21 | علاقات المستخدم في استجابات API أخرى (`Order.assignedUser`، `Document.uploadedBy`، `Branch.users`) كانت تُجلب عبر `include: { ...: true }` الذي يُرجع **كل** حقول نموذج `User` — بما فيها `passwordHash` (كلمة المرور المُشفّرة). | `orders.service.js`، `documents.service.js`، `branches.service.js` | أي مستخدم لديه صلاحية قراءة الطلبات أو المستندات أو الفروع كان يستقبل `passwordHash` الخاص بزملائه ضمن استجابة الـ API مباشرة — ثغرة أمنية حقيقية تسمح بمحاولة كسر كلمات المرور دون اتصال (Offline Cracking). تم تأكيدها فعليًا عبر طلب حقيقي لمسار `/api/documents`. الإصلاح: استبدال `include: { user: true }` بـ `include: { user: { select: safeUserSelect } }` عبر شكل موحّد في `utils/safeSelects.js` يستثني `passwordHash`. |

### 🟢 منخفضة — تحسينات بنيوية

| # | الملاحظة | الموقع |
|---|----------|--------|
| 18 | `docker-compose.yml` لا يحتوي على `healthcheck` لخدمة `postgres` ولا `condition: service_healthy` ضمن `depends_on`. | `docker-compose.yml` — قد يحاول الباك-إند الاتصال بقاعدة بيانات لم تكتمل تهيئتها بعد. |
| 19 | ازدواجية في تنفيذ `prisma generate`: مرة أثناء بناء صورة Docker (`Dockerfile`)، ومرة أخرى عند كل تشغيل حاوية (`docker-compose.yml command`). | `backend/Dockerfile:10` و`docker-compose.yml:26` — غير ضار لكنه تكرار غير ضروري. |

---

## 3. مقترح التطوير

### المرحلة 1 — إصلاحات عاجلة (لتشغيل النظام أصلًا)

1. **توليد وحفظ Migrations فعلية**: تشغيل `npx prisma migrate dev --name init` محليًا لإنشاء أول Migration، والتزامها (commit) داخل `backend/prisma/migrations/`، ثم تعديل أمر تشغيل الحاوية في `docker-compose.yml` إلى:
   `sh -c "npx prisma migrate deploy && npm run dev"`
   (وليس `prisma generate` فقط).
2. **تصحيح `DATABASE_URL` الافتراضي** في `.env.example` ليستخدم `postgres` (اسم الخدمة) بدل `localhost` عند التشغيل عبر Docker Compose، مع توضيح الفرق في `README.md` بين التشغيل المحلي والتشغيل داخل Docker.
3. **توثيق خطوة إنشاء `.env`** بوضوح في `README.md` (نسخ `.env.example` إلى `.env` وتعديل القيم)، أو إضافة سكربت `setup.sh` يقوم بذلك تلقائيًا.
4. **إضافة `healthcheck`** لخدمة `postgres` في `docker-compose.yml` مع `depends_on: condition: service_healthy` لضمان جاهزية قاعدة البيانات قبل بدء الباك-إند.
5. **البدء الفعلي في بناء الواجهة الأمامية** (`request.html`, `admin-dashboard.html` والأصول المرتبطة بها) بما يتوافق مع بيانات `services-data.js` الموجودة فعلًا، أو توضيح خطة تقنية بديلة (React/Vue SPA) إذا كان هذا هو الاتجاه المطلوب.

### المرحلة 2 — إصلاح المنطق والموثوقية

6. استبدال توليد الأرقام التسلسلية القائم على `count()` بحل ذري (Atomic)، إما عبر:
   - عدّاد مخصص في جدول `Setting` يُحدَّث ضمن Transaction بقفل صفوف (`SELECT ... FOR UPDATE`)، أو
   - تسلسل قاعدة بيانات (Postgres `SEQUENCE`) يُستدعى داخل نفس الـ Transaction.
7. إعادة كتابة `recalculateOrderPaymentStatus` لاستبعاد الدفعات ذات الحالة `REFUNDED` (وربما `UNPAID`) من مجموع المبلغ المدفوع، مع إضافة اختبارات تغطي حالات الاسترجاع الجزئي والكلي.
8. تعريف آلة حالات صريحة (State Machine) للطلبات — خريطة انتقالات مسموحة فقط (مثال: `NEW → UNDER_REVIEW → ... → COMPLETED`، ومنع الرجوع لحالات سابقة إلا بصلاحية خاصة) والتحقق منها في `updateOrderStatus` قبل التنفيذ.
9. حذف أحد ملفي `requireRole` المكررين (`role.middleware.js` أو الدالة داخل `auth.middleware.js`) والاكتفاء بمصدر واحد يُستورد في كل الوحدات.
10. تفعيل رفع ملفات حقيقي عبر `multer` (الموجودة أصلًا في `package.json`) مع تخزين محلي مؤقت أو تكامل مع تخزين سحابي (S3-compatible)، والتحقق من نوع/حجم الملف قبل القبول، وربط `documents.controller.js` بهذه الآلية بدل قبول `storagePath` كنص حر من العميل.
11. إضافة Rate Limiting مخصص وأكثر صرامة على `/api/auth/login` (مثلًا 5-10 محاولات لكل 15 دقيقة لكل IP/بريد إلكتروني).

### المرحلة 3 — اكتمال الوظائف الأساسية

12. إضافة عمليات تعديل وحذف (Update/Delete أو Soft-Delete عبر حقل `deletedAt`) لجميع الكيانات الرئيسية (العملاء، الفروع، الموردين، المستندات، العروض، الخدمات).
13. إضافة ترقيم صفحات (`page`/`limit`) وفلاتر بحث لكل نقاط النهاية التي تُرجع قوائم، مع تقليل حجم `include` الافتراضي (جلب التفاصيل الكاملة فقط عند طلب سجل واحد).
14. تحويل حقلي `priority` و`currency` إلى قيم مُقيَّدة (Enum على مستوى Prisma، أو ربط `currency` فعليًا بجدول `Currency` الموجود مسبقًا) بدل نصوص حرة.
15. مزامنة مدة صلاحية كوكي المصادقة مع `JWT_EXPIRES_IN` بدل قيمة ثابتة، وإضافة نقطة نهاية `POST /api/auth/logout` لمسح الكوكي.
16. قراءة كلمة مرور المسؤول الافتراضي في `seed.js` من متغير بيئة (`SEED_ADMIN_PASSWORD`) بدل تثبيتها في الكود، مع فرض تغييرها عند أول تسجيل دخول.
17. تفعيل نموذجي `ActivityLog` و`Notification` فعليًا: تسجيل كل عملية حساسة (تسجيل دخول، إنشاء/تعديل طلب، تغيير حالة دفع) في `ActivityLog`، وإرسال إشعارات داخلية عند أحداث مهمة (تغيير حالة الطلب، استلام دفعة).

### المرحلة 4 — تحسينات إضافية مقترحة للنمو المستقبلي

18. إضافة اختبارات آلية (Unit/Integration Tests) — لا يوجد حاليًا أي مجلد اختبارات في المشروع، ما يجعل أي تعديل مستقبلي عرضة لكسر السلوك الحالي دون اكتشاف مبكر.
19. إضافة إعداد CI (مثل GitHub Actions) يُشغّل `npm install`، فحص الأنماط (Lint)، والاختبارات تلقائيًا عند كل Pull Request.
20. توثيق واجهة الـ API (OpenAPI/Swagger) لتسهيل التكامل مع الواجهة الأمامية القادمة ولتوضيح العقود بين الطرفين.
21. إضافة تقارير مالية ولوحة تحكم أكثر تفصيلاً (إجمالي الإيرادات، المتأخرات، تقارير حسب الفرع/الموظف) بدل الإحصاءات الأساسية الحالية (عدّادات فقط) في `dashboard.service.js`.

---

## 4. الأولوية المقترحة للتنفيذ

```
المرحلة 1 (عاجل جدًا) → المرحلة 2 (خلال أسبوعين) → المرحلة 3 (خلال شهر) → المرحلة 4 (مستمر / حسب نمو المنصة)
```

النظام حاليًا **غير قابل للتشغيل من الصفر** بسبب البند رقم (1) في الأخطاء الحرجة (غياب Migrations)، لذا يُنصح بمعالجة المرحلة 1 بالكامل قبل أي عمل آخر.
