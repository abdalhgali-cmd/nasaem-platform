node_modules/
.env
.env.*
!.env.example
!.env.test.example
npm-debug.log*
.DS_Store
coverage/
uploads/
logs/
.prisma/

# Decompressed OCR trained-data cache written at runtime next to the
# vendored *.traineddata.gz files (see ocr-data/README.md) — regenerated
# from the .gz on first use, not something to commit.
ocr-data/eng.traineddata
ocr-data/ara.traineddata
