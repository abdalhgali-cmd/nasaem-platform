export function serviceImageKey(serviceId) {
  return `service-image-${serviceId}`;
}

export function serviceHeroImageKey(serviceId) {
  return `service-hero-${serviceId}`;
}

export function serviceHeroImageMobileKey(serviceId) {
  return `service-hero-mobile-${serviceId}`;
}

export function serviceMotionVideoKey(serviceId) {
  return `service-motion-${serviceId}`;
}
