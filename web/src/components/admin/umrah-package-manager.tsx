"use client";
/* eslint-disable react-hooks/set-state-in-effect -- initial data loading synchronizes with the API. */

import * as React from "react";
import { Check, Loader2, PackagePlus } from "lucide-react";
import { Button } from "@/components/ui/button";
import { adminRequest } from "@/lib/admin-api";

type PackageService = { id: string; code: string; name: string; category: string; description?: string | null; basePrice: string | number; currency: string; active: boolean; processingTime?: string | null; features?: string[] | null };
const empty = { code: "", name: "", description: "", price: "0", currency: "SAR", duration: "", hotel: "", nights: "", transport: "", visa: "", included: "", excluded: "", active: true };

export function UmrahPackageManager() {
  const [packages, setPackages] = React.useState<PackageService[]>([]); const [form, setForm] = React.useState(empty); const [loading, setLoading] = React.useState(true); const [working, setWorking] = React.useState(false); const [error, setError] = React.useState(""); const [success, setSuccess] = React.useState("");
  async function load() { setLoading(true); try { const payload = await adminRequest<{ data: PackageService[] }>("/services?category=UMRAH_PACKAGE&limit=100"); setPackages((payload.data || []).filter((item) => item.category === "UMRAH_PACKAGE")); } catch (err) { setError(err instanceof Error ? err.message : "تعذر تحميل باقات العمرة"); } finally { setLoading(false); } }
  React.useEffect(() => { void load(); }, []);
  async function create(event: React.FormEvent) { event.preventDefault(); setWorking(true); setError(""); setSuccess(""); try { const features = [`DURATION: ${form.duration}`, `HOTEL: ${form.hotel}`, `NIGHTS: ${form.nights}`, `TRANSPORT: ${form.transport}`, `VISA: ${form.visa}`, ...form.included.split("\n").map((value) => value.trim()).filter(Boolean).map((value) => `INCLUDED: ${value}`), ...form.excluded.split("\n").map((value) => value.trim()).filter(Boolean).map((value) => `EXCLUDED: ${value}`)]; const payload = await adminRequest<{ data: PackageService }>("/services", { method: "POST", body: JSON.stringify({ code: form.code, name: form.name, category: "UMRAH_PACKAGE", description: form.description || null, basePrice: Number(form.price), currency: form.currency, active: form.active, features, processingTime: form.duration || null }) }); setPackages((current) => [payload.data, ...current]); setForm(empty); setSuccess("تم إنشاء باقة العمرة ضمن Service الحالية"); } catch (err) { setError(err instanceof Error ? err.message : "تعذر إنشاء الباقة"); } finally { setWorking(false); } }
  return <section className="space-y-5 rounded-3xl border border-border bg-card p-5 shadow-sm sm:p-7"><div className="flex items-start gap-3"><div className="flex size-11 items-center justify-center rounded-2xl bg-accent/20 text-accent-foreground"><PackagePlus className="size-5" /></div><div><h2 className="text-xl font-black">باقات العمرة</h2><p className="mt-1 text-sm leading-7 text-muted-foreground">تُحفظ الباقة كخدمة من فئة <span className="font-mono" dir="ltr">UMRAH_PACKAGE</span>؛ لذلك تستفيد من السعر والظهور والميزات والصور الموجودة دون Package model مكرر أو inventory غير موجود.</p></div></div>{error ? <div className="rounded-2xl border border-destructive/20 bg-destructive/5 p-4 text-sm font-bold text-destructive">{error}</div> : null}{success ? <div className="rounded-2xl border border-emerald-200 bg-emerald-50 p-4 text-sm font-bold text-emerald-700"><Check className="me-2 inline size-4" />{success}</div> : null}<form onSubmit={create} className="grid gap-4 rounded-2xl bg-muted/50 p-4 sm:grid-cols-2 lg:grid-cols-3"><label className="text-sm font-bold">رمز الباقة<input required placeholder="umrah-economy-2026" value={form.code} onChange={(e) => setForm({ ...form, code: e.target.value })} className="mt-2 h-10 w-full rounded-xl border border-border bg-background px-3 font-normal" dir="ltr" /></label><label className="text-sm font-bold">اسم الباقة<input required value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} className="mt-2 h-10 w-full rounded-xl border border-border bg-background px-3 font-normal" /></label><label className="text-sm font-bold">السعر<input required type="number" min="0" step="0.01" value={form.price} onChange={(e) => setForm({ ...form, price: e.target.value })} className="mt-2 h-10 w-full rounded-xl border border-border bg-background px-3 font-normal" /></label><label className="text-sm font-bold">المدة<input value={form.duration} onChange={(e) => setForm({ ...form, duration: e.target.value })} placeholder="10 أيام" className="mt-2 h-10 w-full rounded-xl border border-border bg-background px-3 font-normal" /></label><label className="text-sm font-bold">الفندق<input value={form.hotel} onChange={(e) => setForm({ ...form, hotel: e.target.value })} className="mt-2 h-10 w-full rounded-xl border border-border bg-background px-3 font-normal" /></label><label className="text-sm font-bold">عدد الليالي<input value={form.nights} onChange={(e) => setForm({ ...form, nights: e.target.value })} className="mt-2 h-10 w-full rounded-xl border border-border bg-background px-3 font-normal" /></label><label className="text-sm font-bold">النقل<input value={form.transport} onChange={(e) => setForm({ ...form, transport: e.target.value })} className="mt-2 h-10 w-full rounded-xl border border-border bg-background px-3 font-normal" /></label><label className="text-sm font-bold">التأشيرة<input value={form.visa} onChange={(e) => setForm({ ...form, visa: e.target.value })} className="mt-2 h-10 w-full rounded-xl border border-border bg-background px-3 font-normal" /></label><label className="flex items-center gap-2 self-end text-sm font-bold"><input type="checkbox" checked={form.active} onChange={(e) => setForm({ ...form, active: e.target.checked })} />ظاهرة للعملاء</label><label className="sm:col-span-2 lg:col-span-3 text-sm font-bold">الوصف<textarea value={form.description} onChange={(e) => setForm({ ...form, description: e.target.value })} className="mt-2 min-h-20 w-full rounded-xl border border-border bg-background p-3 font-normal" /></label><label className="text-sm font-bold">الخدمات المشمولة<textarea placeholder="سطر لكل خدمة" value={form.included} onChange={(e) => setForm({ ...form, included: e.target.value })} className="mt-2 min-h-24 w-full rounded-xl border border-border bg-background p-3 font-normal" /></label><label className="text-sm font-bold">الخدمات غير المشمولة<textarea placeholder="سطر لكل خدمة" value={form.excluded} onChange={(e) => setForm({ ...form, excluded: e.target.value })} className="mt-2 min-h-24 w-full rounded-xl border border-border bg-background p-3 font-normal" /></label><div className="flex items-end"><Button type="submit" disabled={working}>{working ? <Loader2 className="size-4 animate-spin" /> : <PackagePlus className="size-4" />}حفظ الباقة</Button></div></form><div className="grid gap-3 md:grid-cols-2">{loading ? <p className="py-8 text-center text-muted-foreground">جاري التحميل...</p> : null}{packages.map((item) => <div key={item.id} className="rounded-2xl border border-border p-4"><div className="flex items-start justify-between gap-3"><div><p className="font-black">{item.name}</p><p className="font-mono text-xs text-muted-foreground" dir="ltr">{item.code}</p></div><span className="rounded-full bg-primary/10 px-3 py-1 text-xs font-bold text-primary">{Number(item.basePrice).toLocaleString("en-US")} {item.currency}</span></div><p className="mt-3 text-sm text-muted-foreground">{item.description || "بدون وصف"}</p><span className={`mt-3 inline-block rounded-full px-3 py-1 text-xs font-bold ${item.active ? "bg-emerald-500/10 text-emerald-700" : "bg-muted text-muted-foreground"}`}>{item.active ? "نشطة" : "غير نشطة"}</span></div>)}</div></section>;
}
