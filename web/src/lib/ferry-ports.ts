// Ports for the ferry/boat request form — the real Sudan–Saudi Red Sea
// crossing (see FLIGHT_ROUTES' own بورتسودان → جدة entry), plus one
// alternate port on each side. Illustrative placeholders, same as
// flight-routes.ts, not pulled from a live schedule.
export const FERRY_ORIGIN_PORTS = ["بورتسودان", "سواكن"];
export const FERRY_DESTINATION_PORTS = ["جدة", "ينبع"];
